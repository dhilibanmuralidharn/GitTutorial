// initial commit

// updated in feature-1 
for (let index = 0; index < array.length; index++) {
    const eleent = arra[index]
    console.log(eleent)
}
// bug sloved. added extra loop
for (let index = 0; index < array.length; index++) {
    const element = array[index];

}

// update the feature one 1
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    console.log('object')

}

// update the another loop 
for (let index = 0; index < array.length; index++) {
    const element = array[index];

}